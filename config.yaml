workflows:
  - name: "workflow2"
    steps:
      - name: "step1"
        type: "webhook"
        command: "echo 'Hello, world!'"
        target: "http://localhost:8080"
      - name: "step2"
        type: "json"
        command: "data.msg"
        target: "localhost:5514"
      - name: "step3"
        type: "syslog"
        command: "echo 'Goodbye, world!'"
        target: "localhost:5514"